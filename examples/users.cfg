ADMIN_LEVEL is 10;
USER_LEVEL is 1;

@{
    USERS = @{
        ADMIN = "root";
        ADMIN_PRIV = !(ADMIN_LEVEL);
        DEFAULT_USER = "guest";
        DEFAULT_PRIV = !(USER_LEVEL);
    };

    SYSTEM = @{
        VERSION = "1.0.0";
        MAX_SESSIONS = !(5 * 2);
    };
}