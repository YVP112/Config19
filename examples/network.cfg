PORT_BASE is 8000;
OFFSET is 42;

@{
    SERVER_NAME = "main-backend";
    HOST = "127.0.0.1";
    PORT = !(PORT_BASE + OFFSET);

    TIMEOUTS = @{
        CONNECT = 5;
        READ = !(2 * 3);
    };
}