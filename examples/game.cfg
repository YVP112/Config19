BASE_HP is 100;
BONUS_HP is 25;
DMG_MULT is 3;

@{
    GAME_TITLE = "RPG Demo";

    PLAYER = @{
        NAME = "Knight";
        HP = !(BASE_HP + BONUS_HP);
        DAMAGE = !(DMG_MULT * 10);
        CRIT_MOD = !(mod(15, 4));
    };
}